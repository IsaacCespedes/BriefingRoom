// Placeholder SvelteKit config
// This will be properly configured when SvelteKit is initialized in Phase 3
// For now, this file serves as a placeholder for svelte-check

import adapter from "@sveltejs/adapter-auto";

/** @type {import('@sveltejs/kit').Config} */
const config = {
  kit: {
    adapter: adapter(),
  },
};

export default config;

